<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- BIG V Light Theme -->
    <style name="Base.Theme.OnlineShoppingApplication"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Main application background -->
        <item name="android:windowBackground">@color/bigv_cream</item>

        <!-- System status bar -->
        <item name="android:statusBarColor">@color/bigv_brown</item>

        <!-- System navigation bar -->
        <item name="android:navigationBarColor">@color/bigv_brown</item>

        <!-- Status bar text icons -->
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <!-- Application Theme -->
    <style name="Theme.OnlineShoppingApplication"
        parent="Base.Theme.OnlineShoppingApplication" />

</resources>